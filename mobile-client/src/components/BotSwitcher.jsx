import {Search} from 'lucide-react';import {BottomSheet} from './BottomSheet';
export function BotSwitcher({open,onClose,bots,currentId,onSelect}){return <BottomSheet open={open} onClose={onClose} title="切换 Bot"><label className="bot-search"><Search size={16}/><input placeholder="搜索 Bot"/></label><div className="bot-list"><button className={!currentId?'selected':''} onClick={()=>onSelect(null)}><i>✦</i><span><b>自由探索</b><small>暂不绑定 Bot，继续探索</small></span>{!currentId&&<em>✓</em>}</button>{bots.map(bot=><button key={bot.id} className={bot.id===currentId?'selected':''} onClick={()=>onSelect(bot.id)}><i>{(bot.basic?.name||'?')[0]}</i><span><b>{bot.basic?.name||'未命名 Bot'}</b><small>{bot.basic?.intro||'尚未填写简介'}</small></span><em>{bot.id===currentId?'✓':'›'}</em></button>)}</div></BottomSheet>}
