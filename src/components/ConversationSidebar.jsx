import {Box,MessageSquare,Pencil,Plus,Sparkles,Trash2} from 'lucide-react';
export function ConversationSidebar({sessions,activeId,onSelect,onCreate,onRename,onDelete,onOpenBots}){return <aside className="sidebar"><div className="brand"><img className="brand-mark" src="/emochi-logo.png" alt="Emochi"/><span>emochi</span><em>β</em><button className="bot-library-trigger" onClick={onOpenBots}><Box size={15}/><span>Bot 库</span></button></div><button className="new-session" onClick={onCreate}><Plus size={16}/>开始新对话</button><div className="sidebar-section"><p className="side-label">最近对话</p><nav>{sessions.length?sessions.map(s=><div className={`session-item ${s.id===activeId?'active':''}`} key={s.id}><button className="session-row" onClick={()=>onSelect(s.id)}><MessageSquare size={15}/><span><b>{s.title}</b><small>{s.updatedAt}</small></span></button><div className="session-actions"><button aria-label={`重命名 ${s.title}`} title="重命名" onClick={()=>onRename(s)}><Pencil size={13}/></button><button className="delete-session" aria-label={`删除 ${s.title}`} title="删除" onClick={()=>onDelete(s)}><Trash2 size={13}/></button></div></div>):<p className="no-sessions">还没有对话</p>}</nav></div><div className="sidebar-foot"><Sparkles size={14}/><span>创作工作台</span><small>你的对话和工作页会保留在这里</small></div></aside>}
