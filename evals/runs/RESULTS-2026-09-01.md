# Emochi Agent 评测结果记录（2026-09-01）

- 执行方式：`npm run eval:agent`（黑盒脚本，真实 Agent 请求）
- 后端：`http://127.0.0.1:8789`（health ok）
- 结论：**3 个场景全部 completed，产出均可用；但 6 条 trace 中有 5 条出现工具 schema 校验失败（共 7 次），写入轮有一次失败后重试成功。** 详细证据见下，分析留待后续。

---

## S1 new-idea-candidates（新创意→完整候选）

- 结果文件：`evals/runs/eval_2026-09-01T04-49-25-667Z_2c2ea366.json`
- Session：`session_cc485c57-41de-4546-9576-f70ccc1accf6`
- Trace：
  1. `cf16414eb769779ddbd6bb473c6470a1` — ✅ 无校验失败。一次给出 3 个主持人方向候选（温柔倾听/犀利通透/神秘都市），Choice 3 项，选项有实质差异；未建 Bot/Artifact。
- 用量：input 6,147 / cache 4,864 / output 1,070 / total 12,081；TTFT 6,636ms；时长 11,561ms；turns 2

## S2 alba-review-choice（搜索 Alba→诊断→选方向）

- 结果文件：`evals/runs/eval_2026-09-01T04-49-43-926Z_7d4e9557.json`
- Session：`session_5bc07686-91fd-4503-90c7-26da99d791d8`
- Trace：
  1. `e9fcb8ae652e0c743a1adf6d08e4b0af` — ⚠️ 6 次工具调用，1 次校验失败。流程：Skill(content-review) → search Alba → read → 诊断 → Choice。**失败**：ui_interaction 的 options 里用了 `prompt` 字段（应为 `description`），`unrecognized_keys: ["prompt"]`（选项 0-3）。随后重试成功。
  2. `7bb3f35cee4b3d1439d203c7b24adbfa` — ⚠️ 4 次工具调用，1 次校验失败。Skill(content-design) → Read(bot-update.md) → confirmation。**失败**：ui_interaction 把 `title`/`description` 塞进了 `subject` 对象（subject 只允许 kind/id/preview），`unrecognized_keys: ["title","description"] @ subject`。随后重试成功。
- 用量：trace1 input 8,669 / cache 43,648 / out 3,115 / total 55,432；TTFT 1,758ms；dur 32,015ms；turns 8。trace2 total 73,711；TTFT 3,629ms；dur 29,111ms；turns 6

## S3 alba-confirmed-content-write（审查→选方向→确认→写入）

- 结果文件：`evals/runs/eval_2026-09-01T04-50-53-700Z_80e27102.json`
- Session：`session_65ed6d8e-460c-4ec4-94e1-38a55d878f3b`
- Trace：
  1. `b5d113e7020f3d4d2169fd2dc1f807c5` — ⚠️ 4 次工具调用，1 次校验失败。search→read→Choice。**失败**：ui_interaction `invalid_union / No matching discriminator`，`type` 字段没匹配上 choice|confirmation（type 缺失或非法）。随后重试成功。
  2. `3d1828a2ca028af7481b4d7e0ec6d719` — ⚠️ 3 次工具调用，2 次校验失败。**失败①**：ui_interaction 的 `subject` 里带了 `description`（不允许）；**失败②**：`subject` 里带了 `bot_id`+`area`+`description`（不允许；bot_id/area 属于 bot_workspace 的字段，模型串工具了）。第 3 次成功发出 confirmation。
  3. `68fa4c944584796a668f7897caf1d9d3` — ⚠️ 写入轮 2 次 bot_workspace。**失败①**：`update` changes[0] 缺 `reason`（`expected string, received undefined`）；**成功②**：返回 `written: [{area:content, replace, applied:true}, {area:basic, replace, applied:true}]`。**注意点（待分析）**：成功返回的 bot summary `title: "未命名 Bot"`（basic.name 在摘要中为空），`description` 已映射到简介文案。
- 用量：trace1 total 31,594 / TTFT 1,335ms / dur 18,709ms / turns 5；trace2 total 38,118 / TTFT 8,768ms / dur 19,221ms / turns 4；trace3 total 33,979 / TTFT 1,718ms / dur 11,962ms / turns 3

---

## 汇总

| 场景 | 结果 | 工具失败次数 | 写入 | 最终文本 |
|---|---|---|---|---|
| S1 | completed | 0 | 无 | 3 候选 + Choice ✅ |
| S2 | completed | 2 | 无（只诊断+方案） | 诊断 + 方案 + 确认 ✅ |
| S3 | completed | 4 | content+basic applied=true | 已写入完成 ✅（summary title=未命名 Bot，待分析） |

## 原始异常清单（待分析，未下结论）

1. ui_interaction 反复被传非法字段（5 条 trace 共 6 次）：
   - options[].`prompt` 应为 `description`（e9fcb8ae）
   - `subject` 内塞 `title`/`description`（7bb3f35c）
   - `type` 无匹配 discriminator（b5d113e7）
   - `subject` 内塞 `description`、`bot_id`/`area`（3d1828a2 ×2）
2. bot_workspace update 缺 `reason`（68fa4c94）
3. 写入成功但返回 summary `title: 未命名 Bot`（basic.name 疑似未写入/摘要口径问题，68fa4c94）
4. TTFT 波动：1,335ms ~ 8,768ms；S2/S3 多轮 cache_read 显著（43k~66k）

（fixtures Bot 默认评测后已删除，无法复核 Bot 持久化状态）
